import { BaseDestroyDirective } from './base-destroy.directive';

describe('BaseDestroyDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseDestroyDirective();
    expect(directive).toBeTruthy();
  });
});
