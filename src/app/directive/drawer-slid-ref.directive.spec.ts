import { DrawerSlidRefDirective } from './drawer-slid-ref.directive';

describe('DrawerSlidRefDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerSlidRefDirective();
    expect(directive).toBeTruthy();
  });
});
